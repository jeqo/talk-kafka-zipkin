kafka.bootstrap-servers = "localhost:29092"
kafka.bootstrap-servers = ${?KAFKA_BOOTSTRAP_SERVERS}

schema-registry.url = "http://localhost:8081"
schema-registry.url = ${?SCHEMA_REGISTRY_URL}

topics.input-tweets = "twitter_json_v1"